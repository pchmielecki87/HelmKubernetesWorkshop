apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: shop-dev

resources:
  - ../../base

# Obraz budowany lokalnie dla klastra Kubernetes w Docker Desktop
images:
  - name: shop-backend
    newName: shop-backend
    newTag: dev

# Zwiększenie liczby replik na środowisku DEV
patches:
  - target:
      kind: Deployment
      name: shop-backend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
